type Venue {
    id: ID !
    name: String !
    capacity: Int
    cityId: ID
    city: String
    musicGenresIds: [ID]
    musicGenres: [String]
}

extend type Mutation {
    createVenue(venue: VenueDTO !, userId: ID !): Venue
}

input VenueDTO {
    name: String !
    capacity: Int
    cityId: ID
    musicGenresIds: [ID]
}