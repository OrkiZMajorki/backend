type Venue {
    id: ID !
    name: String !
    capacity: Int
    cityId: ID
    city: String
    musicGenresIds: [ID]
    musicGenres: [String]
}

input VenueDTO {
    name: String !
    capacity: Int
    cityId: ID
    musicGenresIds: [ID]
}