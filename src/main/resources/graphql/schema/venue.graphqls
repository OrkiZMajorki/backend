type Venue {
    id: ID !
    name: String !
    capacity: Int
    cityId: ID
    city: String
    musicGenres: [String]
}

input VenueDTO {
    name: String !
    capacity: Int
    cityId: ID
    musicGenres: [String]
}